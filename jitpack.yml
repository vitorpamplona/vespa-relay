jdk:
  - openjdk21
before_install:
  - echo 'allprojects { tasks.matching { it.name.toLowerCase().startsWith("sign") }.configureEach { it.enabled = false } }' > nosign.init.gradle
install:
  - ./gradlew publishToMavenLocal --init-script nosign.init.gradle
